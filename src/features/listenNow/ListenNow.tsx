import { Spinner } from '../../components/elements'

const ListenNow = () => {
  return (
    <h1 className="text-lg underline">
      Player
      <Spinner />
    </h1>
  )
}

export default ListenNow
